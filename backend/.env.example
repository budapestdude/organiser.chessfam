# Server Configuration
NODE_ENV=development
PORT=3000

# Sentry Error Tracking (Backend)
# Get DSN from https://sentry.io
# Create a new project for Node.js and copy the DSN
SENTRY_DSN=https://f14e34e76c4c1da3d6fc2ae894bba464@o4510766621196288.ingest.de.sentry.io/4510766635221072

# Database
DATABASE_URL=postgresql://user:password@localhost:5432/chessfam

# JWT Secrets (REQUIRED - Must be at least 32 characters each)
# Generate with: node -e "console.log(require('crypto').randomBytes(64).toString('hex'))"
JWT_SECRET=your-super-secret-jwt-key-must-be-at-least-32-characters-long
JWT_REFRESH_SECRET=your-super-secret-refresh-key-must-be-at-least-32-characters-long
JWT_EXPIRES_IN=15m
JWT_REFRESH_EXPIRES_IN=7d

# CORS
CORS_ORIGIN=http://localhost:5173

# Resend Email Service (Required for email notifications)
# Get API key from: https://resend.com/api-keys
# 1. Sign up at resend.com
# 2. Go to API Keys page
# 3. Create a new API key
# 4. Verify your domain or use resend's testing domain (onboarding@resend.dev)
RESEND_API_KEY=re_your_api_key_here
EMAIL_FROM=noreply@chessfam.com
EMAIL_FROM_NAME=ChessFam
# For domain verification: https://resend.com/docs/dashboard/domains/introduction

# Stripe (Add when implementing payments)
# STRIPE_SECRET_KEY=sk_test_...
# STRIPE_WEBHOOK_SECRET=whsec_...

# Cloudflare R2 Storage (Optional - falls back to local storage if not configured)
# Get credentials from: Cloudflare Dashboard → R2 → Manage R2 API Tokens
R2_ACCOUNT_ID=your-cloudflare-account-id
R2_ACCESS_KEY_ID=your-r2-access-key-id
R2_SECRET_ACCESS_KEY=your-r2-secret-access-key
R2_BUCKET_NAME=your-bucket-name
# Get public URL from: Cloudflare Dashboard → R2 → Your Bucket → Settings
# Copy the "Public Development URL" (for production, use custom domain instead)
R2_PUBLIC_URL=https://pub-abc123def456.r2.dev

# Google OAuth (Optional - enables Google login)
# Get credentials from: https://console.cloud.google.com/apis/credentials
# 1. Create a new project or select existing
# 2. Enable Google+ API
# 3. Create OAuth 2.0 Client ID (Web application)
# 4. Add authorized redirect URIs:
#    - http://localhost:3000/api/v1/auth/google/callback (development)
#    - https://api.chessfam.com/api/v1/auth/google/callback (production)
GOOGLE_CLIENT_ID=your-google-client-id.apps.googleusercontent.com
GOOGLE_CLIENT_SECRET=your-google-client-secret
GOOGLE_CALLBACK_URL=http://localhost:3000/api/v1/auth/google/callback

# Frontend URL (used for OAuth redirects)
FRONTEND_URL=http://localhost:5173
